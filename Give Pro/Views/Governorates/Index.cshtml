@model IEnumerable<Give_Pro.Models.Governorates>

@{
    ViewBag.Title = "قائمة المحافظات";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>قائمة المحافظات</h2>

<p>
    @Html.ActionLink("إنشاء جديد", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Countries.CountriesName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GovernoratesName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Countries.CountriesName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GovernoratesName)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id = item.Id }) |
            @Html.ActionLink("تفاصيل", "Details", new { id = item.Id }) |
            @Html.ActionLink("حذف", "Delete", new { id = item.Id })
        </td>

    </tr>
}

</table>
